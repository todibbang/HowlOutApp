<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="HowlOut.ManageEventTemplate"
 			 xmlns:common="clr-namespace:UXDivers.Artina.Player;assembly=UXDivers.Artina.Player.Common"
			 xmlns:local="clr-namespace:HowlOut;assembly=HowlOut"
			 xmlns:controls="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin.Abstractions" Padding="0,0,0,10">
	<ContentView.Content>

		<Grid RowSpacing="0" ColumnSpacing="0" Padding="10,0,0,0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="40" />
				<ColumnDefinition Width="40" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="80" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="80"/>
			</Grid.RowDefinitions>

			<StackLayout Grid.Column="1" Grid.ColumnSpan="3" BackgroundColor="White"/>

			<controls:CircleImage Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Source="{Binding ProfileImageSource}" />
			
			<!-- <ContentView Grid.Column="0" Grid.Row="0" x:Name="ProfilView" /> -->
			<!-- <ContentView Grid.Column="2" Grid.Row="0" x:Name="EventView" /> -->

			<StackLayout Grid.Column="2" Grid.Row="0" Padding="10,10,0,0" Spacing="0" >
				<Label Style="{DynamicResource TitleStyle}" Text="Test"/>
				<Label Style="{DynamicResource TimeStyle}" x:Name="time"/>
				<Label Style="{DynamicResource DistanceStyle}" Text="{Binding Distance}" x:Name="distance"/>
			</StackLayout>

			<Button Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="0" x:Name="SubjectButton" BackgroundColor="Transparent" />
		</Grid>
	</ContentView.Content>
</ContentView>
