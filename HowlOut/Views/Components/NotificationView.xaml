<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		x:Class="HowlOut.NotificationView"
		xmlns:controls="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin.Abstractions">
	<ContentView.Content>
			<Grid RowSpacing="0" ColumnSpacing="0" Padding="10,10,0,2" >
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="40" />
					<ColumnDefinition Width="40" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="80"/>
				</Grid.RowDefinitions>

				<StackLayout Grid.Column="1" Grid.ColumnSpan="2" Padding="0,0,0,2" BackgroundColor="{DynamicResource Shadow}">
					<StackLayout Padding="50,2,10,5" VerticalOptions="FillAndExpand" BackgroundColor="White">
						<Label x:Name="Title" TextColor="{DynamicResource HowlOut}" Text="{Binding Title}"/>
						<Label x:Name="Message" FontSize="12" TextColor="{DynamicResource NormalTextColor}" Text="{Binding Message}"/>
						<Label x:Name="time" FontSize="8" TextColor="{DynamicResource NormalTextColor}" VerticalOptions="EndAndExpand" Text="{Binding Time}"/>
					</StackLayout>
				</StackLayout>
				<controls:CircleImage Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" x:Name="image" Source="launch_screen.jpeg" Aspect="AspectFill"/>
			</Grid>
	</ContentView.Content>
</ContentView>
