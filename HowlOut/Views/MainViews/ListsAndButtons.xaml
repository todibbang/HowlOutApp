<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		 x:Class="HowlOut.ListsAndButtons">
	<ContentView.Content>
		
		<Grid VerticalOptions="Fill">
			<Grid.RowDefinitions>
				<RowDefinition Height="*"/>
				<RowDefinition Height="auto"/>
			</Grid.RowDefinitions>

			<ScrollView>
				<Grid  x:Name="thisGrid" VerticalOptions="Fill"/>
			</ScrollView>


			<StackLayout Grid.Row="1" HeightRequest="80" x:Name="findMoreFriends" IsVisible="false">
				<Button Text="Find new friends" x:Name="findFriendsBtn" WidthRequest="250" HorizontalOptions="CenterAndExpand" TextColor="{DynamicResource HowlOut}" BorderColor="{DynamicResource HowlOut}" BorderWidth="1" BorderRadius="10" HeightRequest="40"/>
			</StackLayout>
		</Grid>

	</ContentView.Content>
</ContentView>
